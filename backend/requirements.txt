# GitHub Repository Workflow Backend — Python dependencies
# Pinned to exact versions for reproducible installs.
# Generated for Python 3.12+

# Web framework
fastapi==0.115.6
python-multipart>=0.0.9
pypdf>=5.0.0

# ASGI server
uvicorn[standard]==0.32.1

# Git operations
GitPython==3.1.43

# Data validation
pydantic>=2.10.0

# Pydantic settings (used by rag/config/settings.py for RAG_* env vars)
pydantic-settings>=2.0.0

# Environment variables
python-dotenv==1.0.1

# Testing
pytest==8.3.4
pytest-asyncio==0.24.0
httpx==0.28.1

# Groq API client -- >=0.11.0 fixes httpx 0.28 incompatibility (removed `proxies` kwarg)
#                    <1.0.0   satisfies langchain-groq 0.3.x requirement (requires groq<1)
groq>=0.11.0,<1.0.0

# LangChain & LangGraph ecosystem (v0.3+ for Python 3.13 compatibility)
langchain>=0.3.0
langchain-core>=0.3.0
langchain-community>=0.3.0
langchain-groq>=0.2.0
langchain-google-genai>=2.0.0
google-generativeai>=0.8.0
langgraph>=0.2.0

# Gemini Live API (real-time voice) -- separate SDK from google-generativeai
# above; used only by services/voice_session_service.py for the documentation
# chatbot's voice assistant feature.
google-genai>=2.19.0

# ---------------------------------------------------------------------------
# RAG dependencies
# ---------------------------------------------------------------------------

# FAISS vector store (CPU build -- GPU not required)
faiss-cpu>=1.7.4

# Sentence Transformers -- local embedding provider (no server required)
# Used when RAG_EMBEDDING_PROVIDER=sentence-transformers (default in .env)
sentence-transformers>=2.2.2

# BM25 keyword retrieval
rank-bm25>=0.2.2

# AST parsing via Tree-sitter
tree-sitter>=0.21.0

# Tree-sitter language grammars (Python, Java, JavaScript, TypeScript, C)
tree-sitter-python>=0.21.0
tree-sitter-java>=0.21.0
tree-sitter-javascript>=0.21.0
tree-sitter-typescript>=0.21.0
tree-sitter-c>=0.21.0

# NumPy -- required by FAISS and sentence-transformers
numpy>=1.24.0

# SciPy -- used by eval/paper_table_generator.py for Wilcoxon signed-rank test
# (non-parametric paired comparison at small n for research paper evaluation)
scipy>=1.11.0

# Pinecone vector database client (cloud-hosted vector store)
# Used when RAG_VECTOR_STORE_BACKEND=pinecone in .env
pinecone>=3.0.0

# ---------------------------------------------------------------------------
# Evaluation scripts (scripts/compute_validation_stats.py, etc.)
# ---------------------------------------------------------------------------
pandas>=2.0.0

# ---------------------------------------------------------------------------
# PDF export (Publish page's "Export" section)
# ---------------------------------------------------------------------------
# Markdown -> HTML -> PDF pipeline. xhtml2pdf is pure-Python (no native
# GTK/Pango/Cairo toolchain like WeasyPrint needs), so it installs cleanly
# on Windows without extra system dependencies.
markdown>=3.6
xhtml2pdf>=0.2.16
# pymdownx.superfences: the base `markdown` package's fenced_code extension
# mishandles code fences indented inside list items (common in LLM-generated
# docs, e.g. "1. Do X:\n   ```bash\n   ...\n   ```") -- it falls back to a
# garbled inline <code> span with the language tag leaking into the visible
# text and line breaks collapsed. superfences handles nested/indented fences
# correctly.
pymdown-extensions>=10.0
